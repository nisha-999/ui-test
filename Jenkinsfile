pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Hello Jenkins'
            }
        }

        stage('Test') {
            steps {
                echo 'Pipeline Working'
     pipeline {
    agent any

    stages {

        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }

        stage('Build') {
            steps {
                sh 'npm run build'
            }
        }

    }
}

